[DEFAULT]
# Set up any number of migration data stores you want, one
# The "name" used in the test is the config variable key.
#sqlite=sqlite:///test_migrations.db
sqlite=sqlite://
mysql=mysql+mysqldb://openstack_citest:openstack_citest@localhost/openstack_citest
postgres=postgresql+psycopg2://openstack_citest:openstack_citest@localhost/openstack_citest
